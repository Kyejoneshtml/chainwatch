Shows a transaction's confirmation depth. Zero confirmations is central to the product and gets its own neutral treatment, not a severity colour.

```jsx
<ConfirmationBadge confirmations={0} />
<ConfirmationBadge confirmations={6} />
```
