Standard select, same 36px/6px sizing as `Input` and `Button` for a consistent control row.

```jsx
<Select options={[{ value: 'all', label: 'all severities' }, { value: 'critical', label: 'critical' }]} />
```
