Loading placeholder: `surface-raised` block with a slow opacity pulse. No spinners.

```jsx
<SkeletonBlock width={240} height={14} />
```

Requires the `cw-skeleton-pulse` keyframes (declared once in `guidelines/animation.css`, imported via `styles.css`).
