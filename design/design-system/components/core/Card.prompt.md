Neutral container: white fill, 1px border, 8px radius, 24px padding. The base wrapper for panels and summary blocks.

```jsx
<Card>
  <h3>Case #4471</h3>
  <p>3 addresses linked</p>
</Card>
```

Keep a single accent per card — if a severity badge sits inside, everything else in the card stays neutral.
